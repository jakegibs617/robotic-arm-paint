"""iPhone camera / LiDAR import (Phase 7-8).

Not part of the MVP. We use existing scan apps (Polycam, Scaniverse) to export a
mesh, then import it here. These modules are stubs that define the intended API
and fail with a clear message until the ``scan`` optional dependencies are
installed (``pip install -e ".[scan]"``).
"""
